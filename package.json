{
  "devDependencies": {
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "warning:install": "echo 'remember to run npm install before starting if things have changed'",
    "dev": "npm run warning:install && lein fig:build",
    "build": "lein clean && lein fig:min"
  }
}
